@model BrandViewModel
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="page-content">
        <div class="container-fluid">
            <div class="alert alert-danger" role="alert">
                @Model.ErrorMessage
            </div>
        </div>
    </div>
}
else
{

    <div class="page-content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-xl-12">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title">Araba Markaları Listesi</h4>
                            <div class="table-responsive">
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <table class="table mb-0">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Marka</th>
                                            <th>Sil</th>
                                            <th>Güncelle</th>
                                            <th>Markaya Ait Araçlar</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var brand in Model.Brands)
                                        {
                                            count++;
                                            <tr>
                                                <th scope="row">@count</th>
                                                <td>@brand.Name</td>
                                                <td><a href="/AdminBrand/DeleteBrand/@brand.BrandId" class="btn btn-outline-danger">Sil</a></td>
                                                <td><a href="/AdminBrand/UpdateBrand/@brand.BrandId" class="btn btn-outline-success">Güncelle</a></td>
                                                <td><a href="/AdminBrand/UpdateBrand/@brand.BrandId" class="btn btn-outline-dark">Markaya Ait Araçlar</a></td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                                <a href="/AdminBrand/CreateBrand" class="btn btn-outline-primary">Yeni Marka Oluştur</a>
                            </div>

                        </div>
                        <!-- end card-body-->
                    </div>
                    <!-- end card -->

                </div>
                <!-- end col -->
            </div>
        </div>
    </div>
}

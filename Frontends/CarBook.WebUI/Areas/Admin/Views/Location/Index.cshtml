@model LocationViewModel
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="page-content">
        <div class="container-fluid">
            <div class="alert alert-danger" role="alert">
                @Model.ErrorMessage
            </div>
        </div>
    </div>
}
else
{
    <div class="page-content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-xl-12">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title">Konum Listesi</h4>
                            <div class="table-responsive">
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <table class="table mb-0">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Konum</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var location in Model.Locations)
                                        {
                                            count++;
                                            <tr>
                                                <th scope="row">@count</th>
                                                <td>@location.Name</td>
                                                <td><a href="/Admin/Location/DeleteLocation/@location.LocationId" class="btn btn-danger">Sil</a></td>
                                                <td><a href="/Admin/Location/UpdateLocation/@location.LocationId" class="btn btn-success">Güncelle</a></td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                                <a href="/Admin/Location/CreateLocation" class="btn btn-outline-primary">Yeni Konum Oluştur</a>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}


